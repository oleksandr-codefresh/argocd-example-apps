apiVersion: apps/v1
kind: Deployment
metadata:
  name: project1-ui
  labels: 
    sasd: a
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: project1-ui
  template:
    metadata:
      labels:
        app: project1-ui
    spec:
      containers:
        - image: docker.io/azopat/ks-guestbook-demo:0.1
          name: project1-ui
          ports:
            - containerPort: 80
