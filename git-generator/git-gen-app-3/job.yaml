 apiVersion: batch/v1
 kind: Job
 metadata:
   generateName: schema-migrate-
   annotations:
     argocd.argoproj.io/hook: PreSync
 spec:
   template:
     spec:
       containers:
       - name: sleep-fail
         image: alpine:3.8
         command: [sh, -c, sleep 60]
         args: [exit 0]
       restartPolicy: Never
