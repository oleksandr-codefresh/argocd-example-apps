# apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app: demo

resources:
- deployment.yaml
- service.yaml
- configMap.yaml

images:
  - name: docker.io/xeonalex/guesbook-unprivileged
    newTag: "v0.1"
